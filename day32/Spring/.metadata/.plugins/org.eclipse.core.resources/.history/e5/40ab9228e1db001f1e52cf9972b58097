package com.example.demo.dto;

import com.example.demo.entity.User;

public class UserResDto {

	public static record Login(
			Integer id,
			String nickname
			){
		public Login from(User user) {
			return new Login(user.getId(),user.getNickname());
		}
	}
}
